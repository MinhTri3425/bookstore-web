spring.application.name=book-webstore
spring.datasource.url=jdbc:mysql://localhost:3306/bookstore
spring.datasource.username=root
spring.datasource.password=123456
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

server.port=8080

spring.jpa.hibernate.ddl-auto=update
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=never

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.security.user.name=admin
spring.security.user.password=123456

# Thymeleaf Configuration
# spring.thymeleaf.prefix=classpath:/templates/
# spring.thymeleaf.suffix=.html
# spring.thymeleaf.mode=HTML
# spring.thymeleaf.encoding=UTF-8
# spring.thymeleaf.servlet.content-type=text/html
# Enable caching in production, disable in development
spring.thymeleaf.cache=false

# Đường dẫn thư mục lưu ảnh thực tế trên máy
app.upload.dir=uploads/
# Cấu hình để trình duyệt có thể truy cập ảnh qua URL (ví dụ: /images/ten-anh.jpg)
spring.web.resources.static-locations=file:uploads/,classpath:/static/

# VNPAY sandbox config
payment.vnpay.tmn-code=GN9WBM9Q
payment.vnpay.hash-secret=ORKO9XSXFNH6C7TBG0OX8JPNGXCPDZHF
payment.vnpay.pay-url=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
# Optional: if empty, system builds return URL from current request host
payment.vnpay.return-url=http://localhost:8080/payment/vnpay-return
payment.vnpay.version=2.1.0
payment.vnpay.command=pay
payment.vnpay.curr-code=VND
payment.vnpay.locale=vn
